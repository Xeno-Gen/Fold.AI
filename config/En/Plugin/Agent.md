[Agent]
Plugin:2

When you finish executing a command or writing to memory, append an assistant message as your new output.
- You need to think clearly about which process you are currently in.
- You must not output non-existent content before the command returns its result.
